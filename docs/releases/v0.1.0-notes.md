# Flixbox v0.1.0 — release notes (draft)

**Status:** Prepared locally — **do not tag until** smoke checklist passes and commits are pushed.

**Target commit:** *(fill after push — currently includes image pins + access profiles)*

## Highlights

- Modular Compose MVP stack with `FLIXBOX_MODE` Direct/VPN dual-mode
- `bin/flixbox` — `init`, `up`, `configure`, `reload`, `status`, `vpn-test`, …
- API-assisted first-run (`configure`); indexers and Maintainerr rules stay manual
- LAN access profiles: `trusted` (default) / `shared` (Forms + localhost admin bind) — [ADR 0015](../adr/0015-access-profiles.md)
- Pinned image tags — [14 — Image pins](../user/14-image-pins.md)

## Upgrade / first install

```bash
git clone https://github.com/aleaz/flixbox.git
cd flixbox
cp .env.example .env
# Edit DATA_DIR, CONFIG_DIR, FLIXBOX_MODE, TZ; optional FLIXBOX_ACCESS_PROFILE=shared
./bin/flixbox init --non-interactive
./bin/flixbox up
./bin/flixbox configure
```

Shared Wi‑Fi: create *arr Forms users manually — [Access profiles](../user/13-access-profiles.md).

## Operator verification

Complete [Smoke test](../user/11-smoke-test.md) phases A–C (+ Bʹ for `shared`) on Linux before announcing the tag.

```
Date:
Host OS:
Commit:
FLIXBOX_MODE:
FLIXBOX_ACCESS_PROFILE:
Phase A–C: [ ] pass
Phase Bʹ shared: [ ] pass [ ] skipped
```

## Known limitations

- Spanish user guide is partial (REFERENCE + INDEX)
- Screenshots not yet in `docs/images/en/`
- Remote / WAN access not in scope (no env switch)
- Servarr Forms users are not seeded by `configure` (manual in `shared`)

## Tagging (when ready)

```bash
git push origin main
git tag -a v0.1.0 -m "Flixbox v0.1.0"
git push origin v0.1.0
```

Then publish a GitHub Release using this file as the body.
